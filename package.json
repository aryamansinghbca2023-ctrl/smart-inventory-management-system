{
  "name": "inventory-system",
  "version": "1.0.0",
  "description": "INVORA — Smart Inventory Management System",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "cd client && npm install"
  }
}
